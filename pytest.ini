[pytest]
testpaths = _scripts/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: pure logic, no I/O
    db: needs a Postgres (embedded fixture provides one)
    integration: exercises a script end-to-end
