# `aacapital-delivery` skill review

## Durability

The proposed skill contains only cross-sprint rules: repository/schema evidence and citations; ISIN identity; sample-before-scale; runtime/row/cost gates; dry-run/apply safety; destructive-lineage proof and incident awareness; structural zero-Neon-wake and safe snapshots; Cloudflare Free-tier discipline; one branch/PR; production approval; test/build/rollback proof; CST/CDT and IST labels; and concise owner reporting. These govern any future feature rather than this kickstart alone.

## Deliberately left in this task prompt

Specific route names, snapshot keys, the 2026-08-04 audit sections, P0 task numbering, current measured coverage, the proposed pipeline schedule, exact PR title, this phase's migration fields, and one-time test sequence remain sprint requirements and are not embedded in the skill.

## Overlap

No repository-owned `AGENTS.md` was present at audit time. The skill deliberately overlaps safety principles in `docs/AACAPITAL_KICKSTART_AUDIT.md`, `docs/OWNER_RUNBOOK.md`, and the incident record so future agents load the rules before reading operational documentation. The documents provide phase evidence/procedures; the skill is the compact mandatory policy.

## Recommended location and commit status

The requested repository-local location, `.codex/skills/aacapital-delivery/SKILL.md`, is appropriate because it travels with AACapital and is narrower than a user-global skill. It has been created in the working tree for owner review but is intentionally **not included in the commit or PR without owner approval**. If approved, add it in a later owner-authorized commit rather than silently amending this milestone.
