Status: CURRENT — session record + permanent doctrine (decade lock, source lanes)
Last verified against code: 2026-07-24

# Session Record — 2026-07-22 → 07-24 (the Golden Spine sprint)

## THE DECADE LOCK (owner directive, 2026-07-24 — permanent)
**The accurate dataset and ALL backtests cover 2016 → 2026.** Ten years.
Older rows may exist in the tables — they earn **no backfill effort and no
test weight**. Enforced in code: `rule_validation.py` eligibility carries
`listing_date >= '2016-01-01'`, recorded verbatim in every stored result's
provenance. Every future data effort is scoped to this window.

## Source doctrine (locked 07-23)
| Lane | Source |
|---|---|
| Every IPO detail | **NSE** (nse_issue_info — official, proven from owner's PC) |
| Candles | **Zerodha/Kite** |
| Peer PE comparison | **SBI research notes** |
| Screen info / upcoming discovery | **IPOMatrix** (Chittorgarh retired: same vendor) |

## Backtest-critical vs cosmetic (owner cut, 07-24)
**Critical:** anchor counts (pre-2021 = 1.0% coverage — THE gap), final
subscription numbers (finals only — day-wise scrapped), OFS share %
(derivable now: `ofs_cr/(ofs_cr+fresh_issue_cr)`), candles (done).
**Deferred:** 3-yr financials (no current rule consumes it).
**Cosmetic (fill lazily via existing IPOMatrix `--only-null`):** timetable
dates, lot size, face value, quotas, expenses, GMP history (InvestorGain).

## Coverage truth (owner query on ipo_gold, eligible universe, 07-24)
qib 78.7% · retail 78.8% · total 84.0% · anchor 58.3% · ofs 82.1% ·
**anchor pre-2021: 1.0%** — the entire anchors gap is the pre-2021 void.

## Completed this sprint
- **ONE GOLDEN OBJECT**: durable `ipo_golden` + dynamically generated view
  `ipo_gold` (COALESCE overlaps, rebuild-proof, atomic swap). 763 rows;
  candles_json on 483 IPOs. `consolidate_master.py` automated (pipeline +
  admin job + PC-runnable), executed-tested end-to-end vs real Postgres.
- **Screens read the golden object**: Complete Details binds ISIN, locks,
  mcap, RoNW, P/B, promoter, candle sessions, Sonnet one-line; searchable
  selectors; closest-listed default; search card gains a Details action.
- **Listing Review UI** (D2): 5-session table + evidence-backed
  observations; Live serves IST-today only (stale-card class killed).
- **Rule Validation Engine v1** run on prod (n=462, baseline 75.5%):
  mega-issue beats (85.2%, p=.023); bands validated monotonic (STRONG
  95.5% → AVOID 68.3%); **anchor rules show no edge at current coverage**
  (suspected artifact → hence the NSE backfill).
- **NSE anchor pipeline opened from the owner's PC**: `nse_anchor_backfill`
  (proven session pattern, per-symbol commits, reconnect, throttle-polite).
  Payloads bank ISIN, listing dates, industry, full category subscription
  tables (incl. bNII/sNII + MF line), anchor portions.
- **CI/UAT: 18 → 4 failures.** Root-caused and killed: fixture page-gate,
  chromium projects, artifact-upload YAML (path was EMPTY), event-based
  search navigation (J1), card-copy drift (J5), and **a real integration
  bug** — the journey route's raw neon() client bypassing fixture mode
  (J7's 500; migrated to fixtureAwareNeon). a11y down to named
  color-contrast chips (selectors now ride in the failure message).
- **Pipeline hygiene**: Chittorgarh steps + cron removed (one vendor);
  ntfy push on every run (same topic as login alerts, curl-verified);
  cron twice-daily proven from logs; impossible-OHLC guard; derived
  30/90-day lock dates; password rotation done.

## Tomorrow (07-25)
1. **Mining commit**: nse_issue_info → ipo_golden (broadened anchor
   extraction + activeCat finals top-up + OFS% derivation), scoped 2016+.
2. **Re-grade**: audit → rule_validation rerun (anchors at true coverage;
   OFS-heavy graded first time).
3. **a11y to green**: the named card-chip spans' tokens.
4. **Live screen redesign** (one decision spine, no sticky).
Owner keeps spaced `--limit` passes running; watches ntfy at 17:00 IST.
