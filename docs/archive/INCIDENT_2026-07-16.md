> ARCHIVED DOCUMENT
>
> This file is retained for historical reference only.
> It is not an implementation specification.
> Current product rules are defined in:
> `docs/specifications/AACAPITAL_PRODUCT_CONTRACT.md`

# Incident record — 2026-07-16: the all-awaiting cards
**Symptom:** every command-center card showed awaiting (anchors/OFS/PE/QIB/band/FV);
Live page 500'd. Feared: score/verdict engines lost. **Actual: engines were never
broken** — the enrichment path had 3 stacked identity bugs + the Live route had 2
phantom-column bugs.

## Fix chain (all merged same day)
| PR | Fix | Root cause |
|---|---|---|
| #156 | route ii-join + ingest norm strip ltd/limited | join/matcher treated "Ltd." ≠ "Limited" |
| #157 | SECOND norm() inside resolve_ids | same file had two divergent copies |
| #158 | strip trailing " IPO" (probe-evidenced) | IPOMatrix names end in " IPO"; sym/isin empty pre-listing |
| dedup_merge.py | merged 8 twin pairs (771→763 rows) | NSE discovery created "Limited" twins pre-#155 |
| #159 | live-preopen: fair_value not a DB column | FV is computed in ipo-command TS, never stored |
| #160 | live-preopen: rhp_verdict not a column; whole SELECT schema-verified | RHP verdict lives in ipo_rhp_intel.full_json |

**Deliberately NOT merged:** pair id 861 (RMC) / 908 (UEL) — conflicting symbols =
possibly different companies. MANUAL REVIEW pending; blocks guardrail A's index.

**Correct awaitings (not bugs):** QIB until book close · FV "needs EPS" until
eps_post fix · Alpine/Caliber anchors until IPOMatrix publishes (anchor day).

## Was this redoing PR #148–#155's work? No — completing it.
- #148 was UI (icons/binding/docs) — untouched by this incident.
- #155 fixed the *source* (fetch_nse matches Ltd/Limited before insert) — correct,
  but only for NEW inserts by that one scraper. It could not fix (a) the twins
  already in the table, (b) the route's join, (c) ipomatrix_ingest's two matchers,
  (d) IPOMatrix's " IPO" suffix. #151–#154 (zombie freeze, lean pipeline, RHP
  budget) were orthogonal and survive intact — verdicts/score run in lean L106/L118.
- The overlap lesson: FIVE inline copies of the "same" normalizer had drifted
  (fetch_nse's kept "and"; ingest's kept "IPO"; route kept ltd). Fixing one
  place at a time is whack-a-mole. Hence guardrails below.

## Guardrails (this PR)
- **A — structural:** `canon_company(text)` SQL fn + UNIQUE INDEX on it →
  variant twins become un-insertable; violating scraper errors the same night.
  (Owner-run guardrails_canon.sql; requires RMC/UEL pair resolved first.)
- **B — detection:** twin census wired into check_data_contract.py (nightly
  gate) → exit 1 + named pairs if twins ever reappear.
- **C — the class fix:** ONE canonicalizer `_scripts/lib/canon.py` (+ SQL twin
  in lockstep, \b/\y parity so "Standard" ≠ "Stard"); ipomatrix_ingest (×2) and
  fetch_nse now import it. Route joins intentionally left as-deployed during
  listing week; alignment queued.
