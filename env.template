# /root/aac/.env   — chmod 600 after filling. NEVER commit.
DATABASE_URL=postgresql://<user>:<pass>@<host>/<db>?sslmode=require   # your Neon URL
KITE_API_KEY=br9m41pn8nvvywnl
KITE_API_SECRET=<your kite api secret>
KITE_USER_ID=MNX015
KITE_PASSWORD=<your zerodha login password>
KITE_TOTP_SECRET=<your base32 TOTP secret>
# CRON_SECRET only needed if the VM calls a Vercel /api/cron endpoint (it doesn't by default)
